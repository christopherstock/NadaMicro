pipeline {

    agent any

    stages {

        stage('show environment') {
            steps {
                echo 'PHP version is:'
                sh 'php --version'
                echo 'Node.js version is:'
                sh 'node --version'
            }
        }




    }
}

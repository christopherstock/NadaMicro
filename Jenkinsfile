pipeline {

    agent any

    stages {

        stage('show environment') {
            steps {
                sh 'php --version'
                sh 'node --version'
                sh 'whereis php'
            }
        }




    }
}

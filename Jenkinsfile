pipeline {

    agent any

    stages {

        stage('show environment') {
            steps {
                echo 'I am ..'
                sh 'php --version'

                echo 'PHP version is:'
                sh 'php --version'

                echo 'Node version is:'
                sh 'node --version'
            }
        }




    }
}
